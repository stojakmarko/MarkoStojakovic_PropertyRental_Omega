spring:
  datasource:
    username: 'admin'
    url: jdbc:h2:mem:property-rental
    password: 'admin'
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: testtspring
    password: qrqbrsvecuhxukye
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
property-rental:
  security:
    jwt:
      secret: somesecretkey
      time: 3600
